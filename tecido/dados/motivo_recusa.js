// Tabela 'motivo_recusa' — por que o cortador nao usou a sobra sugerida.
// Cadastro, nunca lista fixa no codigo: o motivo e o diagnostico do
// reaproveitamento, e ele muda com a operacao.
module.exports=require('./_lista')({tabela:'motivo_recusa'});
