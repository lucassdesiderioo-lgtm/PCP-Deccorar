# Segredos - NUNCA versionar
.session_secret
.env

# Banco (PINs dos funcionarios, dados de clientes)
dados.db
dados.db-shm
dados.db-wal
dados.db.*
backups/

# PDFs do Mercado Livre (dados pessoais de compradores)
lotes/

# Planilhas de vendas do Mercado Livre (nome, CPF, endereco do comprador)
# O painel do ML exporta em xlsx e em csv - os tres formatos ficam de fora
*.xlsx
*.xls
*.csv

# Dados de uma rodada de CI ou de um servidor local com PCP_DIR apontado para
# ca (§13). Efemero no runner; aqui aparece so se alguem rodar o mesmo comando.
.dados-ci/

node_modules/

# Backups manuais
*.bak-*
*.bak

*.log
.DS_Store

# A etiqueta do kit enviada pelo admin saiu em 17/09/2026 (fase 4 da spec
# GERADOR-ETIQUETA-KIT): ela era o segundo caminho da mesma etiqueta. A linha
# fica aqui porque a pasta ainda existe no servidor, com o ultimo arquivo
# enviado — o codigo parou de le-la, e nada foi apagado no deploy.
kit/

# fotos da conferencia da coleta (prova do numero do motorista) - FORA DO GIT
coletas/
